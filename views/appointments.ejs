<%- include('partials/header', { user, title: '', path }) %>

<main class="container">
  <section class="hero">
    <p class="kicker">University Guidance Center</p>
    <h1>Scheduled Appointments</h1>
    <p class="muted"><%= user.role === 'admin' ? 'Viewing all appointments (admin).' : 'Viewing your appointments.' %></p>
  </section>

  <% if (error) { %>
    <div class="alert error"><%= error %></div>
  <% } %>

  <div class="card">
    <h2>Book an Appointment</h2>
    <form method="POST" action="/appointments" class="form-grid">
      <div>
        <label for="counselorId">Counselor</label>
        <select id="counselorId" name="counselorId" required>
          <option value="">— Select —</option>
          <% counselors.forEach((c) => { %>
            <option value="<%= c.id %>"><%= c.name %> — <%= c.title %></option>
          <% }) %>
        </select>
      </div>

      <div>
        <label for="date">Date</label>
        <input type="date" id="date" name="date" required>
      </div>

      <div>
        <label for="time">Time</label>
        <input type="time" id="time" name="time" required>
      </div>

      <div class="full">
        <label for="purpose">Purpose</label>
        <input type="text" id="purpose" name="purpose" placeholder="e.g. Academic advising, career assessment..." >
      </div>

      <div class="full">
        <button type="submit" class="btn primary">Schedule Appointment</button>
      </div>
    </form>
  </div>

  <div class="card">
    <h2>Appointment List</h2>
    <% if (appointments.length === 0) { %>
      <p class="muted">No appointments yet. Book one above.</p>
    <% } else { %>
      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th>Date</th>
              <th>Time</th>
              <% if (user.role === 'admin') { %><th>Student</th><% } %>
              <th>Counselor</th>
              <th>Purpose</th>
              <th>Status</th>
              <% if (user.role === 'admin') { %><th>Actions</th><% } %>
            </tr>
          </thead>
          <tbody>
            <% appointments.forEach((a) => {
              const counselor = counselorById(a.counselorId); %>
              <tr>
                <td><%= a.date %></td>
                <td><%= a.time %></td>
                <% if (user.role === 'admin') { %><td><%= a.student %></td><% } %>
                <td><%= counselor ? counselor.name : '—' %></td>
                <td><%= a.purpose %></td>
                <td><span class="badge status-<%= a.status %>"><%= a.status %></span></td>
                <% if (user.role === 'admin') { %>
                  <td class="actions">
                    <% if (a.status === 'scheduled') { %>
                      <form method="POST" action="/appointments/<%= a.id %>/complete">
                        <button type="submit" class="btn btn-sm ok">Complete</button>
                      </form>
                      <form method="POST" action="/appointments/<%= a.id %>/cancel">
                        <button type="submit" class="btn btn-sm danger">Cancel</button>
                      </form>
                    <% } else { %>
                      <span class="muted">—</span>
                    <% } %>
                  </td>
                <% } %>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>
</main>
<%- include('partials/footer', { user }) %>
</body>
</html>